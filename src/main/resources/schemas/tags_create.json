{
  "description": "CRITICAL: ALWAYS call this tool - NEVER make up, guess, or fabricate data. Create new contact tags. IMPORTANT: Before creating a tag, ALWAYS call tags_get_summary with the tagName parameter FIRST to check if the tag already exists. Only create a tag if exists=false. If the tag exists, use tags_assign_contacts instead to add contacts to the existing tag. Use this tool directly only when a single call to this tool is sufficient to complete the user's request; for any sequence of two or more tool calls, use the `batch_action` tool instead.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "partnerClientCode": {
        "type": "string",
        "description": "Optional partner client code to call APIs on behalf of a specific partner office/sub-client. Only use when explicitly requested by the user."
      },
      "tags": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "tagName": {
              "type": "string",
              "description": "Name of the tag. MUST be unique - call tags_get_summary with this name first to verify it doesn't exist."
            },
            "signUpKeyword": {
              "type": "string",
              "description": "Sign-up keyword"
            },
            "shortCodeKeyword": {
              "type": "string",
              "description": "Short code keyword"
            },
            "sendConfirmationText": {
              "type": "integer",
              "description": "Whether to send confirmation text (0 or 1)"
            },
            "confirmationText": {
              "type": "string",
              "description": "Confirmation text message"
            },
            "sendFrom": {
              "type": "integer",
              "description": "Phone number ID to send from"
            }
          },
          "required": ["tagName"]
        }
      }
    },
    "required": ["tags"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "statusCode": {
        "type": "string"
      },
      "message": {
        "type": "string"
      }
    }
  }
}
