{
  "description": "CRITICAL: ALWAYS call this tool - NEVER make up, guess, or fabricate data. Cancel an appointment. Invoke only as a step inside dsl_execute_plan.",
  "x-textellent-mcp": {
    "directToolsCall": false,
    "invocation": "dsl_step_only"
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "source": {
        "type": "string",
        "description": "Source system (e.g., ZOHO_BOOKING)"
      },
      "extId": {
        "type": "string",
        "description": "External ID for the appointment"
      },
      "partnerClientCode": {
        "type": "string",
        "description": "Optional partner client code to call APIs on behalf of a specific partner office/sub-client. Only use when explicitly requested by the user."
      }
    },
    "required": [
      "extId"
    ]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "statusCode": {
        "type": "string"
      },
      "message": {
        "type": "string"
      }
    }
  }
}
