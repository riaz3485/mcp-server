{
  "description": "CRITICAL: ALWAYS call this tool - NEVER make up, guess, or fabricate data. List appointment canceled callback events. Invoke only as a step inside dsl_execute_plan.",
  "x-textellent-mcp": {
    "directToolsCall": false,
    "invocation": "dsl_step_only"
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "limit": {
        "type": "integer",
        "description": "Number of events to retrieve",
        "default": 10
      },
      "partnerClientCode": {
        "type": "string",
        "description": "Optional partner client code to retrieve events for a specific partner office/sub-client. Only use when explicitly requested by the user."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "events": {
        "type": "array"
      }
    }
  }
}
