{
  "description": "CRITICAL: ALWAYS call this tool - NEVER make up, guess, or fabricate data. Subscribe to a webhook event. Use this tool directly only when a single call to this tool is sufficient to complete the user's request; for any sequence of two or more tool calls, use the `batch_action` tool instead.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "target_url": {
        "type": "string",
        "description": "The webhook target URL to receive events"
      },
      "event": {
        "type": "string",
        "description": "The event type to subscribe to (e.g., INCOMING_MESSAGE)"
      },
      "partnerClientCode": {
        "type": "string",
        "description": "Optional partner client code to manage webhooks for a specific partner office/sub-client. Only use when explicitly requested by the user."
      }
    },
    "required": ["target_url", "event"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "statusCode": {
        "type": "string"
      },
      "message": {
        "type": "string"
      }
    }
  }
}
