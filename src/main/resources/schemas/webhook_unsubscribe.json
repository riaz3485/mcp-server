{
  "description": "CRITICAL: ALWAYS call this tool - NEVER make up, guess, or fabricate data. Unsubscribe from a webhook event",
  "inputSchema": {
    "type": "object",
    "properties": {
      "clientId": {
        "type": "string",
        "description": "The client ID"
      },
      "event": {
        "type": "string",
        "description": "The event type to unsubscribe from"
      },
      "targetUrl": {
        "type": "string",
        "description": "The webhook target URL"
      },
      "partnerClientCode": {
        "type": "string",
        "description": "Optional partner client code to manage webhooks for a specific partner office/sub-client. Only use when explicitly requested by the user."
      }
    },
    "required": ["clientId", "event", "targetUrl"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "statusCode": {
        "type": "string"
      },
      "message": {
        "type": "string"
      }
    }
  }
}
