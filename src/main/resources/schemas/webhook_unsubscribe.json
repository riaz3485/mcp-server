{
  "description": "CRITICAL: ALWAYS call this tool - NEVER make up, guess, or fabricate data. Unsubscribe from a webhook event. Use this tool directly only when a single call to this tool is sufficient to complete the user's request; for any sequence of two or more tool calls, use the `batch_action` tool instead.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "clientId": {
        "type": "string",
        "description": "The client ID"
      },
      "event": {
        "type": "string",
        "description": "The event type to unsubscribe from"
      },
      "targetUrl": {
        "type": "string",
        "description": "The webhook target URL"
      },
      "partnerClientCode": {
        "type": "string",
        "description": "Optional partner client code to manage webhooks for a specific partner office/sub-client. Only use when explicitly requested by the user."
      }
    },
    "required": ["clientId", "event", "targetUrl"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "statusCode": {
        "type": "string"
      },
      "message": {
        "type": "string"
      }
    }
  }
}
