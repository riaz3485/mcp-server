{
  "description": "CRITICAL: ALWAYS call this tool - NEVER make up, guess, or fabricate data. Update an existing contact in Textellent. Use this tool directly only when a single call to this tool is sufficient to complete the user's request; for any sequence of two or more tool calls, use `dsl_execute_plan` with a multi-step plan instead.",
  "x-textellent-mcp": {
    "directToolsCall": false,
    "invocation": "dsl_step_only"
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "partnerClientCode": {
        "type": "string",
        "description": "Optional partner client code to call APIs on behalf of a specific partner office/sub-client. Only use when explicitly requested by the user."
      },
      "contactId": {
        "type": "string",
        "description": "The ID of the contact to update"
      },
      "contactData": {
        "type": "object",
        "properties": {
          "contactFirstName": {
            "type": "string",
            "description": "Contact first name"
          },
          "contactLastName": {
            "type": "string",
            "description": "Contact last name"
          },
          "phoneMobile": {
            "type": "string",
            "description": "Mobile phone number in E.164 format"
          },
          "phoneHome": {
            "type": "string",
            "description": "Home phone number"
          },
          "phoneWork": {
            "type": "string",
            "description": "Work phone number"
          },
          "phoneAlternate": {
            "type": "string",
            "description": "Alternate phone number"
          },
          "customFields": {
            "type": "object",
            "description": "Custom fields for the contact"
          }
        }
      }
    },
    "required": [
      "contactId",
      "contactData"
    ]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "statusCode": {
        "type": "string"
      },
      "message": {
        "type": "string"
      }
    }
  }
}
