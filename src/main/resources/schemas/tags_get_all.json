{
  "description": "DEPRECATED: Use tags_get_summary instead for listing tags. This tool is only needed when you require the full list of tag names in a single result. For 'list my tags' or 'show tags' requests, ALWAYS use tags_get_summary which is more efficient. Use this tool directly when it matches the user's request.",
  "x-textellent-mcp": {
    "directToolsCall": false,
    "invocation": "dsl_step_only"
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "limit": {
        "type": "integer",
        "description": "Maximum number of tags to return (default: 50, max: 100). Use pagination for large datasets.",
        "minimum": 1,
        "maximum": 100
      },
      "offset": {
        "type": "integer",
        "description": "Number of tags to skip for pagination (default: 0)",
        "minimum": 0
      },
      "partnerClientCode": {
        "type": "string",
        "description": "Optional partner client code to retrieve tags for a specific partner office/sub-client. Only use when explicitly requested."
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "tagNames": {
        "type": "array",
        "description": "Array of tag names (strings only, not full tag objects). This prevents hallucination by keeping response minimal.",
        "items": {
          "type": "string"
        }
      },
      "total": {
        "type": "integer",
        "description": "Total number of tags available across all pages"
      },
      "limit": {
        "type": "integer",
        "description": "Number of tags returned in this response"
      },
      "offset": {
        "type": "integer",
        "description": "Offset used for this request"
      },
      "hasMore": {
        "type": "boolean",
        "description": "Whether there are more tags to fetch. If true, call again with increased offset."
      }
    },
    "required": ["tagNames", "total"]
  }
}
