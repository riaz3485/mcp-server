{
  "description": "Get a specific contact tag by ID. Use this tool directly only when a single call to this tool is sufficient to complete the user's request; for any sequence of two or more tool calls, use `dsl_execute_plan` with a multi-step plan instead.",
  "x-textellent-mcp": {
    "directToolsCall": false,
    "invocation": "dsl_step_only"
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "tagId": {
        "type": "string",
        "description": "The ID of the tag to retrieve"
      }
    },
    "required": [
      "tagId"
    ]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "tag": {
        "type": "object"
      }
    }
  }
}
