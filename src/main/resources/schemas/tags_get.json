{
  "description": "Get a specific contact tag by ID. Use this tool directly when it matches the user's request.",
  "x-textellent-mcp": {
    "directToolsCall": false,
    "invocation": "dsl_step_only"
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "tagId": {
        "type": "string",
        "description": "The ID of the tag to retrieve"
      }
    },
    "required": [
      "tagId"
    ]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "tag": {
        "type": "object"
      }
    }
  }
}
