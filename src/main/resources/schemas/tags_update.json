{
  "description": "Update an existing contact tag. Use this tool directly when it matches the user's request.",
  "x-textellent-mcp": {
    "directToolsCall": false,
    "invocation": "dsl_step_only"
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "tagId": {
        "type": "string",
        "description": "The ID of the tag to update"
      },
      "tagData": {
        "type": "object",
        "properties": {
          "tagName": {
            "type": "string",
            "description": "Name of the tag"
          },
          "signUpKeyword": {
            "type": "string",
            "description": "Sign-up keyword"
          },
          "shortCodeKeyword": {
            "type": "string",
            "description": "Short code keyword"
          },
          "sendConfirmationText": {
            "type": "integer",
            "description": "Whether to send confirmation text (0 or 1)"
          },
          "confirmationText": {
            "type": "string",
            "description": "Confirmation text message"
          },
          "sendFrom": {
            "type": "integer",
            "description": "Phone number ID to send from"
          }
        }
      }
    },
    "required": ["tagId", "tagData"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "statusCode": {
        "type": "string"
      },
      "message": {
        "type": "string"
      }
    }
  }
}
